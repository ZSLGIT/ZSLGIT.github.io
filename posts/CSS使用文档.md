

### 关于手机端适配
手机端使用vh，计算的高度会包括url输入部分，导致高度比可使用的窗口高，因此会上下滚动，测试使用dvh是可以满足的。

| **单位** | **含义** | **适用场景** |
| :--- | :--- | :--- |
| `vh` | 固定值，基于初始大视口 | 桌面端 OK，手机端有坑 |
| `dvh` | **动态变化**，随地址栏实时调整 | 🏆 移动端做全屏首选 |
| `svh` | 最小可见高度（地址栏展开时） | 需要保证永远不溢出时用 |
| `lvh` | 最大视口高度（地址栏收起时） | 和旧 `vh`基本一致 |

